# size

Returns the number of elements in the array.

```ruby
arr = ['a', 'b', 'c']
arr.size # => 3

# Common use: check if array has items
arr = []
if arr.size > 0
  puts "Array has #{arr.size} elements"
end
```

**Alias:** `length`