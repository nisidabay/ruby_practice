# append

Adds elements to the end of the array (destructive).

```ruby
arr = [1, 2, 3]

# Append single element
arr.append(4) # => [1, 2, 3, 4]

# Append multiple elements
arr.append(5, 6) # => [1, 2, 3, 4, 5, 6]

# Works identically to #push
```

**Alias:** `push`