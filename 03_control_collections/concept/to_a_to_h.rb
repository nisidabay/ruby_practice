#!/usr/bin/env ruby
# to_a method - converts hash to array
# to_h method - converts array to hash

# Thinking in Ruby
#
# to_a and to_h convert between collection types — Hash to Array of pairs,
# and Array of pairs to Hash. Ruby's conversion protocol (to_a, to_h,
# to_s, to_i) is consistent: every object can represent itself as a
# standard type via these methods, enabling polymorphic data
# transformations.
