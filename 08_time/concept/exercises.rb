#!/usr/bin/env ruby
# frozen_string_literal: true

# exercises.rb — Integer and float math, comparisons, predicates

a = 17
b = 5

puts "#{a} + #{b} = #{a + b}"
puts "#{a} / #{b} = #{a / b}"       # integer division — fix this to get 3.4
puts "#{a}.even? => #{a.even?}"     # => false

# --- Write a line that shows a is odd ---

# --- Divide 17 by 5 as a float (hint: .to_f) ---

# --- Is 256 a power of 2? Compute it, don't guess ---
# Hint: keep dividing by 2 until you hit 1 or something odd.

# --- Price calculator: $29.99 with 8% tax. Show the total. ---
